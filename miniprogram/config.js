// API address used by the mini program.
// For local development, keep this as http://localhost:3000 and run the server.
// For real device testing or release, replace it with your deployed HTTPS API domain.
module.exports = {
  API_BASE_URL: 'http://localhost:3000'
};
